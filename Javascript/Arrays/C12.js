//concat ... base value isnt modified. U need to store it in some variable to access the modified string

let l1 = [100,200,300]
console.log(l1)
console.log(l1.concat([11,22]))


let l2 = [10,20]
console.log(l1.concat(l2))
